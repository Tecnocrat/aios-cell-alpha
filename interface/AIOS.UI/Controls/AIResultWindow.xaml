<Window x:Class="AIOS.UI.Controls.AIResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Intelligence Result" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    
    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF007ACC" CornerRadius="5" Padding="15" Margin="0,0,0,20">
            <TextBlock x:Name="TitleText" Text="AI Intelligence Result" Foreground="White" Style="{StaticResource HeaderTextStyle}"/>
        </Border>

        <!-- Result Information -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Status -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Status:" Style="{StaticResource LabelTextStyle}"/>
            <TextBlock x:Name="StatusText" Grid.Row="0" Grid.Column="1" Text="Unknown" FontWeight="Bold"/>

            <!-- Message -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Message:" Style="{StaticResource LabelTextStyle}"/>
            <TextBlock x:Name="MessageText" Grid.Row="1" Grid.Column="1" Text="" TextWrapping="Wrap"/>

            <!-- Timestamp -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Timestamp:" Style="{StaticResource LabelTextStyle}"/>
            <TextBlock x:Name="TimestampText" Grid.Row="2" Grid.Column="1" Text=""/>
        </Grid>

        <!-- Data Content -->
        <GroupBox Grid.Row="2" Header="Result Data" Margin="0,0,0,20">
            <ScrollViewer>
                <TextBox x:Name="DataTextBox" 
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="11"
                         Background="#FFF9F9F9"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CloseButton" Content="Close" 
                    Padding="20,8" Margin="5" Background="#FF007ACC" 
                    Foreground="White" BorderThickness="0" FontWeight="Bold"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>