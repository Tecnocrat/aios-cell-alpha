#include "lattice_space.hpp"
// Implementation currently header-only for LatticeSpace except placeholder file kept
// to allow future heavier logic (energy propagation, mode coupling) without
// altering build structure.

namespace aios::lattice {
// Intentionally empty – all inline / header for now.
}
